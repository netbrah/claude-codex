{
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "pattern": "^/[a-z]+/.*\\.rs$",
      "description": "Rust source file path"
    },
    "filter": {
      "type": "object",
      "properties": {
        "regex": {
          "type": "string",
          "pattern": "^[a-zA-Z].*",
          "description": "Search regex"
        }
      }
    }
  }
}
